# Dev / test dependencies. Install with:
#   .venv/bin/pip install -r requirements.txt -r requirements-dev.txt
#
# Production code MUST NOT import these — they exist only for tests.

# v0.21.4: synthetic-PDF generation for endpoint integration tests
# (test_bulk_upload_endpoint.py builds real PDFs in memory to exercise
# the actual extraction + classification path under load).
reportlab>=4.0.0

pytest>=7.0.0
pytest-timeout>=2.1.0
